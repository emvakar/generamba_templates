{% include 'header' %}

import SwiftUI

struct {{ module_info.name }}UIView: View {
    @State var viewModel: {{ module_info.name }}ViewModel

    var body: some View {
        Text("{{ module_info.name }}")
    }
}

#Preview {
    {{ module_info.name }}UIView(viewModel: .init(performAction: { _ in }))
}
